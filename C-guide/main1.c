#include <stdio.h>

/**
 *main: this main is just to print text to the screen
 *
 *description- the print is a already declared function in our stdio header
 *		that helps it print out input to the standard output
 *
 *Return: return 0 on success
 */

int main()
{
	printf("This printf function prints to the screen");
	return (0);
}
